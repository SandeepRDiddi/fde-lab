# Scenario engine (backend/) base URL — server-side only, never exposed to the browser.
BACKEND_URL=http://localhost:8000

# AI persona service (persona-service/) base URL — server-side only.
PERSONA_SERVICE_URL=http://localhost:8001
